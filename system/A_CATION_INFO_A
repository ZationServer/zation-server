Name        : Zation
Version     : 0.7.9.7
Status      : Alpha
Author      : Luca Scaringella
Description : Application Server Framework

TODO

-SERVER-
IF(V-0.9)
ADD file support (ADD validator Files)
ADD amazon s3 support
END

IF(V-1)
ADD Config check
?-ADD User can add things to a Bag before work on an request / Support for other connections/Services
?-ADD Multi Service Support -> more sql/mailer services with keys. No key use first service
?-ADD One of 2 optional params is needed
?-Change http session to jwt token? YES
?-Add controller access for user with id
END

-CLIENT-
IF(V-0.8)
Http support
UserGroup to UserType
END


Import from modules by create Http server?

Client->



Zation.sendHttp(controllerName,params,reaction,token);
zation.getJwtToken();


zation-client

zation-server

zation

zation-client-http


Application
Zation

Aplivela   |
Conside    |
Xation     |
Zation     |
Extration  |
Zappli     |
Aplita     |
Intox      |
U-Trap     |







